How to use:
    1. pip install pgzero (a version of Pygame, a library for making games in Python)
    2. Locate the package directory and replace pgzero contents with the pgzero from this directory
    3. Run the game using RunTheGame.sh 

